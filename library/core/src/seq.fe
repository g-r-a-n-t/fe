/// Trait for types that can be iterated over.
pub trait Seq<T> {
    /// Returns the length of the sequence.
    fn len(self) -> usize

    /// Returns the element at the given index.
    fn get(self, i: usize) -> T
}

// NOTE: Array Seq implementation (`impl<T, const N: usize> Seq<T> for [T; N]`)
// is intentionally omitted. Const generics in array type positions are not yet
// fully supported. Arrays are handled as a special case in the type checker.
