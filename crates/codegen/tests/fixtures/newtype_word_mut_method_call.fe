struct Wrap {
    inner: u256,
}

impl Wrap {
    fn bump(mut self) -> Self {
        self.inner += 1
        self
    }
}

pub fn newtype_word_mut_method_call(x: u256) -> u256 {
    let mut w = Wrap { inner: x }
    w = w.bump()
    w.inner
}
