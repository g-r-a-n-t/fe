Added `assert_msg(cond, "message")`, which reverts with a Solidity-compatible `Error(string)` payload (selector `0x08c379a0`) when `cond` is false. String literals satisfy the generic `String<N>` parameter without an explicit annotation.
